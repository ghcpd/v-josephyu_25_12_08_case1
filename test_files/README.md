This folder contains example test files for manual/tested use.

- test_api.py: Demonstrates programmatic register/login using requests and BeautifulSoup. Requires the app to be running locally (python app.py) and `requests` and `beautifulsoup4` installed in the environment.

To run:

pip install requests beautifulsoup4
python test_api.py
